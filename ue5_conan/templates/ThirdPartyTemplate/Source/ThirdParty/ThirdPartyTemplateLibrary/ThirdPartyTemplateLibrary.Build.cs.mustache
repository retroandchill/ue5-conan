// Fill out your copyright notice in the Description page of Project Settings.

using System.IO;
using UnrealBuildTool;

public class {{library_plugin_name}} : ModuleRules
{
	public {{library_plugin_name}}(ReadOnlyTargetRules Target) : base(Target)
	{
		Type = ModuleType.External;
        {{#include_dirs}}
		PublicSystemIncludePaths.Add("$(ModuleDir)/{{dirname}}");
        {{/include_dirs}}

        {{#link_libraries}}
        PublicAdditionalLibraries.Add("$(ModuleDir)/{{library_name}}");
        {{/link_libraries}}

        {{#shared_libraries}}
        PublicDelayLoadDLLs.Add("{{simple_name}}");
        RuntimeDependencies.Add("$(ModuleDir)/{{library_name}}");
        {{/shared_libraries}}
	}
}
